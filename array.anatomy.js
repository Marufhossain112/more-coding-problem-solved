//! includes
let numbers = [54,32,56,43,23,76];
console.log(numbers.includes(32));/* returns true */
// ! indexOf
console.log(numbers.indexOf(52));/* returns -1 cause 52 is missing from the array */
//! concat
let newNumbers = [343,5655,343,4,4];
console.log(numbers.concat(newNumbers));

